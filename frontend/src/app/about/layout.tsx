export default function AboutLayout({ children }: { children: React.ReactNode }) {
  return (
    <>
      {/* This renders the content of the about page inside the Root Layout */}
      {children}
    </>
  );
}